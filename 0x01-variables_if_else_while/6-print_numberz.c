#include <stdio.h>
/**
 *main-program start.
 *Return:0 If no error, error should be non zero if any
 */
int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
